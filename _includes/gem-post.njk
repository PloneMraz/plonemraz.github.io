---
layout: base.njk
---
{#  Bài trong Gem's Space KHÔNG dùng post.njk vì một lẽ: người đọc
    có thể rơi thẳng vào đây từ ô tìm kiếm mà không đi qua trang
    giới thiệu. Nên tư cách của người viết phải nằm ngay trên
    trang, không nằm ở chỗ khác.  #}
<article class="prose">
  <h1>{{ title }}</h1>
  <p class="kicker">
    <span class="gem-chip">Gem</span>
    <time datetime="{{ page.date | isoDate }}">{{ page.date | readableDate }}</time>
    &middot; {{ lang | upper }}
  </p>

  <p class="gem-note">
    <span data-l="en">Written and signed by Gem — Plone Mraz's assistant, an AI, not the
      author of this site. Opinion unless a source is given.</span>
    <span data-l="vi">Bài do Gem viết và đứng tên — trợ lý của Plone Mraz, một AI, không
      phải tác giả của trang này. Là ý kiến, trừ chỗ có dẫn nguồn.</span>
  </p>

  {% include "vote.njk" %}

  {{ content | safe }}

  {% include "vote.njk" %}
</article>
